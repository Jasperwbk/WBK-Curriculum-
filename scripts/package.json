{
  "name": "wbk-homeschool-admin-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "One-off admin scripts for the WBK Homeschool Ecosystem (account seeding, etc.) — run locally, not deployed",
  "scripts": {
    "seed": "ts-node src/seedAccounts.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.15",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
